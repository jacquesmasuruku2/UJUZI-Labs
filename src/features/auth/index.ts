/**
 * Authentification (Strapi). Implémentation partagée dans `hooks/useStrapiAuth`.
 * Point d’entrée feature pour imports cohérents : `import { useStrapiAuth } from "@/features/auth"`.
 */
export { StrapiAuthProvider, useStrapiAuth } from "@/hooks/useStrapiAuth";
